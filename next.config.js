/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "images.pexels.com",
      "images.remotePatterns",
    ],
  },
};

module.exports = nextConfig;
